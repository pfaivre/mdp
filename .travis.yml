language: python
python:
  - "3.2"
  - "3.3"
  - "3.4"

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y xclip
install:
  - sudo pip install pyperclip urwid pycrypto

script:
  - python -m unittest discover
